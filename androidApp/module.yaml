product: android/app

dependencies:
  - //shared
  - $libs.androidx.activity.compose
  - $compose.preview
  - $compose.uiTooling

settings:
  compose: enabled
  android:
    namespace: org.example.project
    applicationId: org.example.project
