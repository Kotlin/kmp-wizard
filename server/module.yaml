product: jvm/app

dependencies:
  - //core
  - $ktor.server.core
  - $ktor.server.netty
  - $libs.logback.classic

settings:
  ktor: enabled
  jvm:
    mainClass: org.example.project.server.ApplicationKt

test-dependencies:
  - $ktor.server.testHost
